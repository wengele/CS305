// input cost of house
// process we calculate the downpayment based on formula
// out downpayment.
/*function calDownpayment(cost) {
    let downPayment;
    let rate = 1 / 100;
    if (cost < 50000) {
        downPayment = 5 * rate * cost
    } else if (cost < 100000) {
        downPayment = 1000 + 10 * rate * (cost - 50000)
    } else if (cost < 200000) {
        downPayment = 2000 + 15 * rate * (cost - 100000)
    } else {
        downPayment = 5000 + 10 * rate * (cost - 200000)
    }
    return downPayment;
}
*/

